import br.ifpb.diagnosticos.sistema.SistemaExamesMedicos;

/**
 * Classe principal simplificada para execução direta
 * Uso: javac -cp src Main.java && java -cp src:. Main
 */
public class Main {
    public static void main(String[] args) {
        // Delegar para a classe principal do sistema
        SistemaExamesMedicos.main(args);
    }
}
